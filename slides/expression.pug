extends layout.pug 

block vars
  - var title = 'expression'

block title
        |  JSX is an Expression Too
block content
      pre.fragment
        code.js 
          | function getGreeting(user) {
          |    if (user) {
          |      return &lt;h1&gt;Hello, {formatName(user)}!&lt;/h1&gt;;
          |    }
          |    return &lt;h1&gt;Hello, Stranger.&lt;/h1&gt;;
          |  }

      p.credits
        a(href="https://reactjs.org/docs/introducing-jsx.html") Introducing JSX
